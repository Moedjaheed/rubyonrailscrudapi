<%= form_with(model: kendaraan, local: true) do |form| %>
  <% if kendaraan.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(kendaraan.errors.count, "error") %> prohibited this kendaraan from being saved:</h2>

      <ul>
      <% kendaraan.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :employee_id %>
    <%= form.text_field :employee_id %>
  </div>

  <div class="field">
    <%= form.label :merk_kendaraan %>
    <%= form.text_field :merk_kendaraan %>
  </div>

  <div class="field">
    <%= form.label :tipe_kendaraan %>
    <%= form.text_field :tipe_kendaraan %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
